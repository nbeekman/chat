extends ../layout

block content
  h2= user.displayName
  img(src="/images/#{user.avatar}", width="200")
  form(action="/account",method="post",enctype="multipart/form-data")
    label Upload an Image
    input(type="file",name="image")
    input(type="submit",value="Save")